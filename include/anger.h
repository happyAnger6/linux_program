#ifndef _ANGER_H
#define _ANGER_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include <unistd.h>
#include <errno.h>


#define min(a,b) ((a) < (b)? (a):(b))
#define max(a,b) ((a) > (b)? (a):(b))

#endif
